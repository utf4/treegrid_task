export interface modalData {
  id: string;
  field: string;
  headerText: string;
  width: string;
  textAlign: string;
  dataType: string;
  bgColor: string;
  fontSize: string;
  fontColor: string;
  format: string;
}
